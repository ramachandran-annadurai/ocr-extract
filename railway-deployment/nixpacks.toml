[phases.setup]
nixPkgs = ["python311", "ccache", "build-essential"]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "python --version",
  "ccache --version"
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
